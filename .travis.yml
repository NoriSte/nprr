language: node_js
node_js:
  - 8
  - 10
  - 12

before_script:
  - npm pack
  - npm i -g $(find . -name 'npr-*') # install the package globally

jobs:
  include:
    node_js: node
    script: npr test

after_success: npm run semantic-release
